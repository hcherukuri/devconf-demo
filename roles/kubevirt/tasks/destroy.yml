---
- name: Delete VM
  kubevirt.core.kubevirt_vm:
    name: "{{ item.name }}"
    namespace: "{{ namespace }}"
    state: absent
    wait: yes
  loop" "{{ vm }}"

- name: Delete namespace
  kubernetes.core.k8s:
    name: "{{ namespace }}"
    api_version: v1
    kind: Namespace
    state: absent